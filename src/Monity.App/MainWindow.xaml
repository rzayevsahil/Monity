<Window x:Class="Monity.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Monity.App"
        mc:Ignorable="d"
        Title="Monity - App Usage Tracker"
        Height="600" Width="900"
        MinHeight="400" MinWidth="600"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}"
        Closing="Window_Closing">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource CardBrush}" Padding="16,12" BorderBrush="#E2E8F0" BorderThickness="0,0,0,1">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Image Source="pack://application:,,,/Monity.App;component/Assets/Logo.png" Width="32" Height="32" Margin="0,0,10,0" Stretch="Uniform"/>
                    <TextBlock Text="Monity" FontSize="20" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Border x:Name="UpdateBanner" Visibility="Collapsed" Background="#E0F2FE" CornerRadius="6" Padding="10,6" Margin="0,0,12,0" VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock x:Name="TxtUpdateMessage" Text="Yeni sürüm mevcut" Foreground="{StaticResource TextBrush}" FontSize="12" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <Button x:Name="BtnUpdate" Content="Güncelle" Style="{StaticResource PrimaryButtonStyle}" Padding="10,6" Click="BtnUpdate_Click"/>
                        </StackPanel>
                    </Border>
                    <Button x:Name="BtnSettings" Content="Ayarlar" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,8,0" Click="BtnSettings_Click"/>
                    <Button x:Name="BtnMinimizeToTray" Content="Tray'e Küçült" Style="{StaticResource SecondaryButtonStyle}" Click="BtnMinimizeToTray_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Content -->
        <Frame x:Name="MainFrame" Grid.Row="1" NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>
