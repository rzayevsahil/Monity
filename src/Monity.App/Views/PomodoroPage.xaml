<Page x:Class="Monity.App.Views.PomodoroPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="400" d:DesignWidth="500"
      Title="{DynamicResource Pomodoro_Title}"
      Background="{DynamicResource BackgroundBrush}">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="24" HorizontalAlignment="Center" MaxWidth="400">
            <TextBlock Text="{DynamicResource Pomodoro_Title}" FontSize="22" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,24" HorizontalAlignment="Center"/>
            <Border Background="{DynamicResource CardBrush}" CornerRadius="12" Padding="32" Margin="0,0,0,24" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                <StackPanel>
                    <TextBlock x:Name="TxtPhase" Text="" FontSize="14" Foreground="{DynamicResource TextMutedBrush}" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                    <TextBlock x:Name="TxtCountdown" Text="25:00" FontSize="48" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" HorizontalAlignment="Center" Margin="0,0,0,24"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button x:Name="BtnStartWork" Content="{DynamicResource Pomodoro_StartWork}" Style="{DynamicResource PrimaryButtonStyle}" Margin="0,0,8,0" Click="BtnStartWork_Click"/>
                        <Button x:Name="BtnStartBreak" Content="{DynamicResource Pomodoro_StartBreak}" Style="{DynamicResource SecondaryButtonStyle}" Margin="0,0,8,0" Click="BtnStartBreak_Click"/>
                        <Button x:Name="BtnPause" Content="{DynamicResource Pomodoro_Pause}" Style="{DynamicResource SecondaryButtonStyle}" Margin="0,0,8,0" Click="BtnPause_Click" Visibility="Collapsed"/>
                        <Button x:Name="BtnResume" Content="{DynamicResource Pomodoro_Resume}" Style="{DynamicResource SecondaryButtonStyle}" Margin="0,0,8,0" Click="BtnResume_Click" Visibility="Collapsed"/>
                        <Button x:Name="BtnStop" Content="{DynamicResource Pomodoro_Stop}" Style="{DynamicResource SecondaryButtonStyle}" Click="BtnStop_Click" Visibility="Collapsed"/>
                    </StackPanel>
                </StackPanel>
            </Border>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,8">
                <TextBlock Text="{DynamicResource Pomodoro_WorkMinutes}" Foreground="{DynamicResource TextMutedBrush}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <Border Width="60" Background="{DynamicResource CardBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6">
                    <TextBox x:Name="TxtWorkMinutes" Padding="8,6" BorderThickness="0" Background="Transparent" Foreground="{DynamicResource TextBrush}" VerticalAlignment="Center" Text="25"/>
                </Border>
                <TextBlock Text="{DynamicResource Pomodoro_BreakMinutes}" Foreground="{DynamicResource TextMutedBrush}" VerticalAlignment="Center" Margin="24,0,8,0"/>
                <Border Width="60" Background="{DynamicResource CardBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6">
                    <TextBox x:Name="TxtBreakMinutes" Padding="8,6" BorderThickness="0" Background="Transparent" Foreground="{DynamicResource TextBrush}" VerticalAlignment="Center" Text="5"/>
                </Border>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>
