<Page x:Class="Monity.App.Views.AchievementsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:Monity.App.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="{DynamicResource Achievement_Title}"
      Background="{DynamicResource BackgroundBrush}">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="24">
            <Grid Margin="0,0,0,16">
                <TextBlock Text="{DynamicResource Achievement_Title}" FontSize="24" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" VerticalAlignment="Center"/>
                <Button x:Name="BtnBack" HorizontalAlignment="Right" Content="{DynamicResource Main_Back}" Style="{DynamicResource SecondaryButtonStyle}" Padding="12,6" Click="BtnBack_Click"/>
            </Grid>
            
            <ItemsControl x:Name="AchievementsItemsControl">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border x:Name="CardBorder" Background="{DynamicResource CardBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="12" Padding="20" Margin="0,0,16,16" Width="240" Height="220">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- Icon -->
                                <Border Grid.Row="0" Width="48" Height="48" CornerRadius="24" Background="{Binding IconBackground, Mode=OneWay}" HorizontalAlignment="Left" Margin="0,0,0,12">
                                    <Viewbox Width="24" Height="24">
                                        <Path Data="{Binding IconPath, Mode=OneWay}" Fill="{Binding IconColor, Mode=OneWay}"/>
                                    </Viewbox>
                                </Border>

                                <!-- Title -->
                                <TextBlock Grid.Row="1" Text="{Binding Title}" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource TextBrush}" TextWrapping="Wrap" Margin="0,0,0,4"/>
                                
                                <!-- Description -->
                                <TextBlock Grid.Row="2" Text="{Binding Description}" FontSize="12" Foreground="{DynamicResource TextMutedBrush}" TextWrapping="Wrap" LineHeight="16"/>

                                <!-- Progress and Status -->
                                <StackPanel Grid.Row="3" Margin="0,12,0,0">
                                    <Grid Margin="0,0,0,4">
                                        <TextBlock Text="{Binding ProgressText, Mode=OneWay}" FontSize="11" Foreground="{DynamicResource TextMutedBrush}"/>
                                        <TextBlock Text="{Binding StatusText, Mode=OneWay}" FontSize="11" FontWeight="SemiBold" Foreground="{Binding StatusBrush, Mode=OneWay}" HorizontalAlignment="Right"/>
                                    </Grid>
                                    <ProgressBar Height="6" Value="{Binding ProgressValue, Mode=OneWay}" Minimum="0" Maximum="100" 
                                                 Background="{DynamicResource BorderBrush}" Foreground="{Binding StatusBrush, Mode=OneWay}" 
                                                 BorderThickness="0" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</Page>
