<Window x:Class="Monity.App.Views.ScreenshotDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource Screenshot_Title}"
        Height="520" Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource BackgroundBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <RadioButton x:Name="RadioVisible" Content="{DynamicResource Screenshot_VisibleArea}" Foreground="{DynamicResource TextBrush}" FontSize="14" Margin="0,0,0,6" IsChecked="True" Checked="Option_Changed"/>
            <RadioButton x:Name="RadioFullPage" Content="{DynamicResource Screenshot_FullPage}" Foreground="{DynamicResource TextBrush}" FontSize="14" Margin="0,0,0,0" Checked="Option_Changed"/>
        </StackPanel>
        <TextBlock Grid.Row="1" Text="{DynamicResource Screenshot_Preview}" Foreground="{DynamicResource TextBrush}" FontSize="13" Margin="0,0,0,6"/>
        <Border Grid.Row="2" Background="{DynamicResource CardBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="10" Margin="0,0,0,16">
            <Image x:Name="PreviewImage" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"
                   MaxHeight="320" RenderOptions.BitmapScalingMode="HighQuality"/>
        </Border>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="{DynamicResource Main_Cancel}" Style="{DynamicResource SecondaryButtonStyle}" Padding="12,8" Margin="0,0,8,0" IsCancel="True" Click="BtnCancel_Click"/>
            <Button x:Name="BtnSave" Content="{DynamicResource Screenshot_Save}" Style="{DynamicResource SecondaryButtonStyle}" Padding="12,8" Margin="0,0,8,0" Click="BtnSave_Click"/>
            <Button x:Name="BtnCopy" Content="{DynamicResource Screenshot_Copy}" Style="{DynamicResource PrimaryButtonStyle}" Padding="12,8" Click="BtnCopy_Click"/>
        </StackPanel>
    </Grid>
</Window>
